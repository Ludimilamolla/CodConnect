import tkinter as tk
from funcionalidade import *

def criar_interface():
    root = tk.Tk()
    root.title("CodConnect")

    # Defina os elementos da interface gráfica, como botões, campos de entrada, listas, etc.
    # Use funções da camada de funcionalidade para realizar ações quando os botões forem clicados.

    root.mainloop()

if __name__ == "__main__":
    criar_interface()
